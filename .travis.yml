sudo: required
language: java
notifications:
  email: false
env:
- PROFILE=$([ "$TRAVIS_BRANCH" == "master" ] && echo "prod" || echo "dev")
before_install:
- openssl aes-256-cbc -K $encrypted_a6a69c5b1756_key -iv $encrypted_a6a69c5b1756_iv
  -in secrets/secrets.tar.enc -out secrets.tar -d
- tar xvf secrets.tar
install:
- mvn -P $PROFILE install -DskipTests=true -Dmaven.javadoc.skip=true -B -V
script:
- mvn -P $PROFILE test -B
before_deploy:
- cp target/moltimate-backend-1.0.0.war moltimate-backend.war
deploy:
- provider: gae
  skip_cleanup: true
  project: moltimate
  keyfile: client-secret.json
  on: master
- provider: gae
  skip_cleanup: true
  project: moltimate
  version: development
  no_promote: true
  keyfile: client-secret.json
  on: dev
- provider: gae
  edge: false
  skip_cleanup: true
  project: moltimate
  version: demo
  no_promote: true
  keyfile: client-secret.json
  on: MOL-36
